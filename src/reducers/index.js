const initialState = {

};

export const carReducer = (state = initialState, action) => {
    switch(action.type) {
        // case: 
        //     return
        default:
            return state;
    }
}